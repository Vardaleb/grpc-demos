name: events
description: Library for the generated protobuf/gRPC classes
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.3.4

# Add regular dependencies here.
dependencies:
  fixnum: ^1.1.0
  grpc: ^3.2.4
  protobuf: ^3.1.0
  # path: ^1.8.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
